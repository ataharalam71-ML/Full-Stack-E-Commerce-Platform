# Used automatically by `npm run build` (Vite reads .env.production for builds).
# "/api" = the site and the API are served from the same origin, which is how the
# single-service deploy and `cloudflared tunnel` work. A real environment variable set
# in your host's dashboard overrides this file, so split hosting still works.
VITE_API_URL=/api
