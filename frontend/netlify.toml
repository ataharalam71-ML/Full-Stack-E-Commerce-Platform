[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every unknown path must fall back to index.html so /deal/... and
# /admin work when opened directly or refreshed.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
